# {{project_name}}

Micronaut 4 application generated by jloom. The entry point is
`{{package}}.Application`; the HTTP server is Netty (default).

## Build

```
./gradlew build
```

## Run

```
./gradlew run
```

The server listens on `:8080` by default (override via `MICRONAUT_SERVER_PORT`).

## What's in here

- `src/main/java/{{package_path}}/Application.java` — entry point
- `src/main/resources/application.yml` — Micronaut config namespace (`micronaut:`)
- `build.gradle` — Micronaut Application plugin (Groovy DSL — Micronaut's default)
- `settings.gradle` — `rootProject.name`

## Next steps

Add capabilities via `jloom add <module>` once the corresponding Micronaut-specific
modules are available in the catalog.