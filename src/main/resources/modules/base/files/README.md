# {{project_name}}

Generated by `jloom new`. This project is managed by [jloom] — see `.jloom/state.json` for
which modules have been applied. Add more capabilities with:

```
jloom add <module>[,<module>...]
jloom list
jloom info <module>
```
