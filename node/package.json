{
  "name": "jloom",
  "version": "0.2.0",
  "description": "Generate and evolve production-ready backends from a composable module catalog.",
  "type": "module",
  "bin": {
    "jloom": "./src/cli.ts"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "start": "node src/cli.ts",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.18.0",
    "typescript": "^5.8.0"
  }
}
