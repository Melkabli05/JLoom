- org.openrewrite.yaml.MergeYaml:
    key: $
    yaml: |
      spring:
        datasource:
          url: jdbc:h2:mem:{{db_name}};DB_CLOSE_DELAY=-1
          username: sa
          password: ""
        jpa:
          open-in-view: false
    filePattern: "**/application.yml"
