{
  "name": "jloom",
  "version": "0.2.0",
  "description": "Generate and evolve production-ready backends from a composable module catalog.",
  "type": "module",
  "bin": {
    "jloom": "./src/cli.ts"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "compile": "bun build src/cli.ts --compile --outfile dist/jloom && cp -R catalog dist/catalog"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "commander": "14",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.8.0"
  }
}
