/**
 * Application layer: use-case orchestration and service interfaces. Depends on the domain
 * layer only — infrastructure and presentation depend on this layer, never the reverse.
 */
package {{package}}.application;
